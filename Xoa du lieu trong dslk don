//na
#include<bits/stdc++.h>
using namespace std;
#define ll long long int

int main(){
    int n, x; 
    cin >> n; 
    int a[100005]= {}; 
    for (int i = 0; i<n; i++) cin >> a[i]; 
    cin >> x; 
    for (int i = 0; i<n; i++){
        if ( a[i] == x) continue; 
        else cout << a[i] << " "; 
    }
    return 0;
}
