//na
#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main(){
    int n; cin >> n; 
    vector<ll> a(n+5); 
    ll sum = 0; 
    for (int i = 0; i<n; i++) {
        cin >> a[i]; 
        if (a[i] > 0 ) sum += 2*a[i]; 
    }
    cout << sum << endl; 
    return 0;
}
